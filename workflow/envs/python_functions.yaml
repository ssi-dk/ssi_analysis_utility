name: python_functions
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - pandas
  - pyyaml
  - biopython
  - pip
  - pip:
      - typer  # optional if you later switch to it