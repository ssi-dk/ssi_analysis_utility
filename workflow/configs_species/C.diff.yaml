analyses_to_run: 


    bowtie2:
        options: --very-sensitive-local --no-unal -a 

    KMA_filter:
        options : --organism 'Clostridioides difficile'

    plasmidfinder:
        options:

    resfinder:
        options:

    virulencefinder:
        options:

    serotypefinder:
        options:

    amrfinder:
        options: --organism 'Clostridioides_difficile'

    mlst:
        options:

    CDiff_Repeat_identifier:
        repeats: TR6 TR10
        combos: TRST 

    samtools:
        view: 
            options: -q 20 

        sort: 
            options: -n 
    
    bcftools:
        view:
            region: AM180355.1_tcdC_804309_805008
            options: -v indels -i 'INFO/DP>10'

    Deletion_identifier:
        options : --organism 'Clostridioides difficile' --deletion_region_buffer 5 --partial_overlap 0.3
    
    SNP_identifier:
        options : --organism 'Clostridioides difficile'


